<h1>Blocks Example</h1>

{{! #each, #if, #unless, are all Handlebar Helpers }}
<ul>
    {{#each tours}}
            {{! I'm in a new block, and context has changed }}
            <li>
                <span class="bolden">{{name}}</span> - {{price}}
                {{#if ../currencies}}
                    {{! Jeff-chopped one '../' in the book example, because it was wrong}}
                    ({{../currency.abbrev}})
                {{/if}}
            </li>
        {{/each}}
</ul>


{{!inverted if}}
{{#unless currencies}}
    <p>All prices in {{currency.name}}.</p>
{{/unless}}

{{#if specialsUrl}}
    {{!I'm in a new block... but context hasn't changed (sort of?) }}
    <p>Check out our <a href="{{specialsUrl}}">specials!</a></p>
{{/if}}

<p>
    {{#each currencies}}
        <a href="#" class="currency">{{.}}</a>
    {{else}}
        Unfortunately, we currently only accept {{currency.name}}
    {{/each}}
</p>


{{#if videoGames}}
    <h3>Favorite games</h3>
    <ol>
        {{#each videoGames}}
        {{! . (period) refers to the current item in the list}}
            <li>
                <a href="http://www.makerjeff.com/{{.}}">{{.}}</a>
            </li>
        {{/each}}
    </ol>
{{/if}}
